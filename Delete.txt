{links.map(({ name, path }, index) => (
                            <li key={index}>
                                <a href={path}>{name}</a>
                            </li>
                        ))}

firebase

// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
const firebaseConfig = {
  apiKey: "AIzaSyDMpF9IVEVsl1RrkYb37nWZHJfsz8wDK2E",
  authDomain: "digital-adventures-5db34.firebaseapp.com",
  projectId: "digital-adventures-5db34",
  storageBucket: "digital-adventures-5db34.firebasestorage.app",
  messagingSenderId: "499278122231",
  appId: "1:499278122231:web:eb5f59707915cc035457f2"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);